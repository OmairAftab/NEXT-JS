export default function AboutPage() {

    console.log("By default every page in next.js is server component and aso when its when we open console in browser then server keyword will be written before it")

    return (
        <div>
            <h1>About Page</h1>
        </div>
    )
}